interface:
  display_name: "Spec-Driven Development"
  short_description: "Plan and approve a change, or verify one end to end"
  default_prompt: "Use $spec-driven-development to turn my rough feature idea into an approval-gated specification and implementation plan, or run /spec check on a PR, ticket, or diff to produce a Verification Brief."
policy:
  allow_implicit_invocation: true
